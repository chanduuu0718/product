# Application scaffold

The application code will be organized into public and admin experiences. Keep reusable UI components separate from data access and authentication logic.

Public routes should remain accessible without login. Admin routes must be protected.
